// Global 
  // Local 
    // Local
  // Local

  //let name = 'Jeevan'

  if (true) {
      //
      // let name = 'Puhan'
  
      if (true) {
          name = 'Jen'
          console.log(name)
      }
  }
  
  if (true) {
      console.log(name)
  }

  // leaded global - when we assign a value to a variable but that was never explicitly
  //                 defined. so in this case we have a new global name